<%= render 'shared/errors', obj: @status %>

<% if logged_in? %>
  <%= form_for @status do |f| %>
  <%= f.label :body %>
  <%= f.text_area :body %>
  <br>
  <%= f.submit "Tweet!" %>
  <% end %>
<% end %>
